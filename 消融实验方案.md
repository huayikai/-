# 主实验：三方对比消融方案（我的方法 vs DVD vs BM）

> 目标：证明**我的方法 = DVD 去混淆 + BM 非单调**的组合，优于只有一半的两个基线。
> 前提：三方配置全在 **main 分支**，**直接可跑，无需统一分支**（我的方法取第一层基础组合，不含三创新）。

---

## 1. 对比设计（核心逻辑）

| 方法 | 配置文件 | learner | mixer | abs | = 包含什么 |
|---|---|---|---|---|---|
| **我的方法** | `dvd+qmix_without_abs` | `dvd_nq_learner_with_sarsa` | `dvd` | False | DVD去混淆 + 非单调 + SARSA + TD(λ) + RND |
| **DVD** | `dvd` | `dvd_learner` | `dvd` | True | 只有 DVD 去混淆（单调、Q-learning、无 RND） |
| **BM** | `qmix_without_abs` | `nq_learner_with_sarsa` | `qmix_without_abs` | False | 只有非单调 + SARSA + TD(λ) + RND（无去混淆） |
| （下界）QMIX | `qmix` | `nq_learner` | `qmix` | True | 标准基线 |

**对比读法**：

- **我的方法 vs BM** → 差异是**DVD 去混淆** → 证明"去混淆有用"
- **我的方法 vs DVD** → 差异是**去 IGM 非单调化**（+SARSA+RND） → 证明"去单调有用"
- **三方一起** → 证明"组合 > 任一单独一半"

> ⚠️ 诚实备注（备答辩）：DVD 对照用 max target/无 RND，BM 对照与我方法的 RND 实现/target 更新略有差异。因此"我的方法 vs DVD/BM"的差距是**一揽子差异**，非单一变量。毕业论文可接受此口径；若要严格归因某一因素，需另做控制变量消融。

---

## 2. 通用设置

- **种子**：每方 5 个（`seed=1..5`），取均值 ± 跨种子标准差
- **环境分层**（先便宜后贵）：
  1. `one_step_matrix_game`（秒级，直接暴露相对过泛化）
  2. `stag_hunt`（分钟级，过泛化经典）
  3. SMAC 硬图：`3s5z_vs_3s6z`、`MMM2`、`6h_vs_8z`（credit assignment 重要，DVD 优势区；**避开 corridor**）
- **覆盖语法**：Sacred `with key=value`
- **结果**：`results/`（`tensorboard --logdir=results/tb_logs`）

---

## 3. 运行命令

### 3.1 过泛化小环境（先跑这个，快）

```bash
# ---- stag_hunt ----
python main.py --config=dvd+qmix_without_abs --env-config=stag_hunt with seed=1   # 我的方法
python main.py --config=qmix_without_abs     --env-config=stag_hunt with seed=1   # BM
python main.py --config=dvd                   --env-config=stag_hunt with seed=1   # DVD
python main.py --config=qmix                   --env-config=stag_hunt with seed=1   # QMIX(下界,可选)

# ---- one_step_matrix_game ----
python main.py --config=dvd+qmix_without_abs --env-config=one_step_matrix_game with seed=1
python main.py --config=qmix_without_abs     --env-config=one_step_matrix_game with seed=1
python main.py --config=dvd                   --env-config=one_step_matrix_game with seed=1
python main.py --config=qmix                   --env-config=one_step_matrix_game with seed=1
```

### 3.2 SMAC 硬图（说服力实验，算力充裕时）

```bash
# 以 3s5z_vs_3s6z 为例；MMM2 / 6h_vs_8z 替换 map_name
python main.py --config=dvd+qmix_without_abs --env-config=sc2 with env_args.map_name=3s5z_vs_3s6z seed=1
python main.py --config=qmix_without_abs     --env-config=sc2 with env_args.map_name=3s5z_vs_3s6z seed=1
python main.py --config=dvd                   --env-config=sc2 with env_args.map_name=3s5z_vs_3s6z seed=1
python main.py --config=qmix                   --env-config=sc2 with env_args.map_name=3s5z_vs_3s6z seed=1
```

---

## 4. 批量脚本（多方法 × 多种子）

**PowerShell**（Windows）：
```powershell
$methods = @("dvd+qmix_without_abs","qmix_without_abs","dvd","qmix")
$seeds   = 1,2,3,4,5
$env     = "stag_hunt"
foreach ($m in $methods) {
  foreach ($s in $seeds) {
    Write-Host ">>> $m seed=$s on $env"
    python main.py --config=$m --env-config=$env with seed=$s
  }
}
```

**Bash**：
```bash
methods=("dvd+qmix_without_abs" "qmix_without_abs" "dvd" "qmix")
env="stag_hunt"
for m in "${methods[@]}"; do
  for s in 1 2 3 4 5; do
    echo ">>> $m seed=$s on $env"
    python main.py --config=$m --env-config=$env with seed=$s
  done
done
```

> SMAC 版把 `--env-config=$env` 换成 `--env-config=sc2 with env_args.map_name=3s5z_vs_3s6z seed=$s`。

---

## 5. 预期与解读

| 对比 | 成功信号 | 若不达预期 |
|---|---|---|
| 我的方法 vs BM | 我的方法 ≥ BM，过泛化环境上明显 | 若持平 → DVD 增益被非单调掩盖，聚焦 matrix/stag_hunt 等去混淆重要的环境 |
| 我的方法 vs DVD | 我的方法 > DVD（尤其非单调最优解不满足 IGM 的任务） | 若 DVD 更好 → 该任务过泛化不严重，单调已够 |
| 我的方法 vs QMIX | 我的方法 ≫ QMIX | 若接近 → 检查是否真的进入过泛化区 |
| 跨种子稳定性 | 我的方法 std 更小（DVD 多头方差缩减，定理2） | std 大 → 检查学习率/初始化 |

**主打环境**：相对过泛化最典型的是 `one_step_matrix_game`（可直接看收敛到哪个联合动作）和惩罚版 `stag_hunt`。这两个上赢得干净，比 SMAC 更能说明"解决了过泛化"。

---

## 6. 后续可扩展（本方案之外，按需）

三方对比跑通、确认方法有效后，可再补细粒度消融支撑各设计选择：
- **层次放置**：`with dvd_layer=second`（第一层 vs 第二层，已实现，接证明 L1/L2）
- **多头数**：`with dvd_heads=1/2/4/8`（验证方差缩减定理2，复现 DVD 原文 Fig.5）
- **三创新 leave-one-out**：需 feature 分支（`use_abs_anneal`/`use_causal_explore`/`use_head_competition`）
- **RND on/off**：`with use_rnd=False`

这些不属于"三方对比"，列此备忘。
