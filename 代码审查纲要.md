# DVD + 非单调 QMIX 代码审查纲要

> 审查对象：`D:\src`（PyMARL 框架定制版）
> 核心配置：`config/algs/dvd+qmix_without_abs.yaml`
> 理论基础：DVD（ICML 2022, *Deconfounded Value Decomposition for MARL*）+ Beyond Monotonicity（AAAI 2026 oral, *Revisiting Factorization Principles in Multi-Agent Q-Learning*）
> 审查日期：2026-08-31

---

## 0. 一句话总结

代码正确实现了「DVD 去混淆结构（轨迹图 G + 多头后门调整）」与「无单调约束（abs=False）+ SARSA 目标 + TD(λ) + RND 探索」的组合，训练主链路无致命错误；**主要问题集中在实验公平性**（两版 RND 不对称、软/硬目标更新不一致、RND β 与论文差 50 倍）**和死配置**（causal loss 未实现）。

---

## 1. 项目全景与算法定位

| 配置 | learner | mixer | abs | 目标更新 | RND | 定位 |
|---|---|---|---|---|---|---|
| `dvd+qmix_without_abs.yaml` | `dvd_nq_learner_with_sarsa` | `dvd` (DVDMixer) | False | 软更新 τ=0.01（每步） | 归一化+衰减 β | **主实验**：DVD 去混淆 × 去 IGM |
| `qmix_without_abs.yaml` | `nq_learner_with_sarsa` | `qmix_without_abs` (Mixer) | False | 硬更新 200ep | 原始 MSE 恒定 β | **对照**：纯 Beyond Monotonicity |
| `dvd.yaml` | `dvd_learner` | `dvd` | True（默认） | 硬更新 200ep | 无 | 原始 DVD（论文复现） |
| `Kaleidoscope+DVD.yaml` | `KaleidoscopeDVDLearner` | `dvd` | True（默认） | 硬更新 | 无 | 另一条研究线（掩码共享+多样性） |

研究问题（我的理解）：**去除 IGM 约束后，DVD 的轨迹图/后门调整能否缓解相对过泛化**——即把 BM 论文的"动力学自纠偏"与 DVD 的"因果去混淆"叠加，观察是否优于纯无约束 QMIX。

---

## 2. 训练数据流（dvd+qmix_without_abs）

```
batch: (B, T_max, ...) — actions 为 buffer 中实际执行动作（parallel_runner 确认）
│
├─ RND（use_rnd=True, β 线性衰减 0.01→0 @6M步）
│    s_{t+1} → prediction error → RMS 归一化 → clip ±5 → rewards += β·r_int
│    （与论文 Algorithm 1 的 r_int,t = ||ĝ(s_{t+1})−g(s_{t+1})||² 对齐）
│
├─ Online MAC 前向 T_max 步 → mac_out(B,T,N,A) + hidden_states(B,T,N,64) [clone，保梯度]
│    chosen_q = gather(mac_out[:, :-1], actions)  # (B, T-1, N)
│
├─ Target MAC 前向（no_grad）→ 同样收集 hidden
│    SARSA: target_q = gather(target_mac_out[:, 1:], actions[:, 1:])  # 实际执行动作
│    target_q_tot = target_DVDMixer(target_q, s[:,1:], target_hidden[:,1:])
│
├─ targets = build_td_lambda_targets(λ=0.3)  # TD(λ)，基于 SARSA 值
│
├─ online_q_tot = DVDMixer(chosen_q, s[:, :T-1], hidden[:, :-1])
│
└─ loss = MSE(online_q_tot − targets) → 反传（agent RNN 同时受 Q 头与 GAT 路径的梯度）
```

维度核对：所有张量在 T_min 处对齐，TD(λ) 递归、mask/terminated 处理与 PyMARL 官方一致。**主链路正确**。

---

## 3. 论文 → 代码映射

### 3.1 DVD 论文（ICML 2022）

| 论文公式 | 代码位置 | 状态 |
|---|---|---|
| 式(8) h'_i = σ(Σ α_ij W h_j) | `dvd.py` MultiHeadGAT.forward（matmul(attention, h_prime) + ELU） | ✅ |
| 式(9)(10) α_ij softmax / e_ij=LeakyReLU(a[Wh_i‖Wh_j]) | 同上（拼接后 sum + LeakyReLU(0.2)） | ✅ |
| 轨迹图 G：RNN 隐状态为节点、全连接 | learner 收集 `mac.hidden_states` 逐时间步 | ✅ |
| 式(11) K_d = \|f_s(s)·G_d\| | `w1_state @ graphs_T` → `w1_heads`；`abs=True` 时取绝对值 | ✅（abs=False 时去掉，即去 IGM） |
| 式(12) K = (1/D)Σ K_d | `w1 = w1_heads.mean(dim=1)`（多头即蒙特卡洛采样 D=dvd_heads=8） | ✅ |
| 式(13) Q_tot = Σ_j k_j·Q̂_j | `hidden=ELU(Q·W1+b1)`（即 Q̂）+ `y=hidden·W_final+V` | ✅ |
| D=8（SMAC）/ D=4（MACO） | `dvd_heads: 8` | ✅ |
| 训练用 TD-loss（式14） | loss_td | ✅ |

注：论文中 `|·|` 同时承担**单调性**职责（原文：*"|·| ... is used to fulfill the monotonicity constraint"*）；abs=False 移除的正是这一部分，与你的研究意图一致。

### 3.2 Beyond Monotonicity 论文（AAAI 2026）

| 论文要点 | 代码位置 | 状态 |
|---|---|---|
| 无约束 mixer（去掉非负权重约束） | DVDMixer `abs=False` + W1/W_final 初始化 ×0.01（防初始震荡，合理的工程处理） | ✅ |
| SARSA 目标 y = r + γQ(s', a')（去 max） | `dvd_nq_learner_with_sarsa.py` gather(buffer 实际动作) | ✅ |
| TD(λ) 平滑 n 步 SARSA 回报 | `build_td_lambda_targets`，λ=0.3 | ✅ |
| RND 内在奖励 r = r_ext + β·r_int，输入 s_{t+1} | RNDModel + `rewards += β·r_int` | ✅（但 β 数值与论文不符，见 §4.1-3） |
| ε-greedy 探索（论文指出近似贪心探索是理论前提） | `epsilon_greedy`，ε 1.0→0.05 @500K | ✅ |
| 论文 Table 2：Adam, lr=1e-3, RND lr=5e-4 | 配置一致 | ✅ |

---

## 4. 问题清单

### 4.1 一致性问题（实验公平性）— **优先处理**

1. **RND 实现两版不对称（高）**
   - 主实验（DVD 版）：原始误差 → RunningMeanStd 归一化 → clip ±5 → β 线性衰减（0.01→0 @6M）
   - 对照组（`nq_learner_with_sarsa`）：**原始 MSE、不归一化、不裁剪、β 恒定 0.01**
   - 后果：两组的内在奖励尺度/时间分布完全不同，"DVD 是否有增益"的结论被探索信号差异污染，不是干净对照。

2. **目标网络更新机制两版不一致（高）**
   - DVD 版：软更新 τ=0.01（每步），`target_update_interval: 200` 是死配置
   - 对照组：硬更新（每 200 episodes 拷贝）
   - 两篇论文都是周期性硬拷贝；软更新偏离论文且与对照组机制不同 → 对比不干净。

3. **RND β 与论文差 50 倍（高）**
   - 论文 Table 2：**β 从 0.5 退火到 0.05，100K 步**（"Anneal start weight: 0.5, anneal step: 100K, finish: 0.05"）
   - 你的配置：`rnd_beta: 0.01`，6M 步衰减到 0
   - 即便考虑归一化改变尺度，0.01 vs 0.5 仍差一个量级以上，且 100K vs 6M 的退火节奏完全不同。论文的直觉是"前期强探索、后期弱探索"，当前配置探索强度可能整体偏弱。建议做 β 消融（0.01/0.1/0.5 + 论文式退火）。

4. **`use_rnd` 门控不一致（中）**
   - 对照组 learner 无条件启用 RND（无 `use_rnd` 检查），主实验版有门控 → 想关闭 RND 做消融时对照组无法关闭。

### 4.2 潜在 Bug

5. **`dvd_nq_learner_with_sarsa.py:63-64`（中，潜伏）**：`mixer == "qmix_without_abs"` 分支调用 `Mixer(args)`，而 `nmix.Mixer.__init__(args, abs=True)` 默认 `abs=True`——**名字叫 without_abs 实际是单调 QMIX**。对照组 `nq_learner_with_sarsa.py:50-52` 正确传了 `Mixer(args, abs=use_abs)`。当前配置走 `dvd` 分支不触发，但任何人切这个 mixer 就会静默得到单调版本。

6. **日志统计口径（低）**：两版 learner 的 `q_taken_mean` 一个除以 `mask.sum()`、另一个除以 `mask.sum() * n_agents`（dvd_learner 老版）——TensorBoard 曲线不可直接对比（当前两个 sarsa 版口径一致，老 dvd_learner 不同）。

### 4.3 死配置 / 清理项

7. **`use_causal_loss: True` / `lambda_causal: 0.1`（`dvd.yaml`）没有任何代码读取**——注释"TD Loss + Causal Loss"与实际不符。若 causal 正则已被放弃，请删除配置并修改注释；若未放弃，需补实现。
8. `dvd.yaml` 中 `mixer:` 键重复出现（line 26 与 47），YAML 后者覆盖前者，无害但应清理。
9. `q_lambda: False`、`target_update_interval`（DVD+SARSA 版）、`state_shape: 0` 均为死配置（`state_shape` 由 run.py 自动覆盖，注释已说明，仅提醒）。
10. `build_td_lambda_targets` 在两个 learner 中重复定义，`utils/rl_utils.py` 已有官方版——建议复用，避免两处演化不一致。

### 4.4 与论文超参数的其它偏差（仅供参考）

- Buffer/batch：论文 5000/128（BM）、5000/32（DVD）；你的 2500/64 —— 因 `batch_size_run: 4` 的并行采样限制，属正常取舍，但复现论文曲线时注意。
- λ=0.3：论文正文未给出 TD(λ) 的 λ 值，0.3 是自选（QMIX 默认 0.6），建议注明。
- 硬更新 vs 软更新的效果差异未验证——若软更新是刻意的，建议在论文中说明理由。

---

## 5. 研究设计点评（DVD × 去 IGM 的合理性）

1. **理论相容性**：BM 的定理（Thm 2/3）只要求 `f_mix` 局部 Lipschitz + Jacobian 满秩（局部非退化）+ 低温度近似贪心探索，不依赖 mixer 的具体结构。DVDMixer（GAT 权重生成器 + ELU + V 偏置）满足这些条件，**理论结论可迁移**——这是组合方案站得住脚的基础。
2. **DVD 的去混淆在 abs=False 下依然成立**：后门调整 = 对轨迹图 G 的蒙特卡洛积分（多头均值），与 `|·|` 无关；去掉的只是单调性。所以"去 IGM + 保留去混淆"在因果意义上自洽。
3. **一个值得警惕的耦合**：DVD 的轨迹图 G 直接依赖 agent 的 RNN 隐状态（即策略本身），而 BM 的"近似贪心探索"也是由该策略诱导的——去混淆与探索策略之间存在间接耦合，实验结论（"增益来自去混淆而非图消息传递"）需要 DVD 论文式消融支撑（D=1 vs D=8 对比已具备条件：`dvd_heads` 直接可调）。
4. **论文写作建议**：BM 论文的消融（Figure 6）显示"RND/SARSA 加在单调 QMIX 上无益"，你的实验矩阵恰好可以补上一个关键格：**DVD（单调, abs=True）× RND/SARSA 组件**，以证明增益来自"去 IGM × 去混淆"的交互而非组件叠加。

---

## 6. 建议的下一步（按优先级）

1. **统一实验基线**：把对照组 RND 改成与主实验相同的归一化+衰减（或都关掉 RND 跑一组），目标更新统一为硬更新或都软更新 → 得到干净对照。
2. **修复潜伏 bug**：`dvd_nq_learner_with_sarsa` 的 `qmix_without_abs` 分支补 `abs=use_abs`。
3. **清理死配置**：删除 `use_causal_loss`/`lambda_causal` 或补实现；删除重复 `mixer` 键。
4. **β 消融**：按论文 (0.5→0.05 @100K) 与当前 (0.01→0 @6M) 各跑一组，确认探索强度选择。
5. **补充消融格**：`dvd_heads` 1 vs 8（验证后门调整的作用）；单调 DVD × SARSA/RND（验证交互效应）。
6. **对照实验矩阵建议**：QMIX（单调基线）/ NM-QMIX（BM 复现）/ DVD-QMIX（原论文）/ DVD+NM（你的方法），同一超参数同一 seed 集（≥5 seeds）。
