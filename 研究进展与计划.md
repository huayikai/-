# DVD × 非单调 QMIX 研究进展与计划

> 更新日期：2026-09-24
> 最新完整实验数据、公式和命令见 `实验上下文总结.md` 第 11 节。本文档后半部分保留早期研究计划作为历史记录；当前决策以本节为准。

## 2026-09-24 当前决策摘要

### 方法没有被替换

原始 Base DVD 及其在 `6h_vs_8z` 上的有效结果继续保留。Residual DVD、Credit DVD、Scheduled Takeover 也均保留为消融。当前新增 `Adaptive Takeover`，目的是解决 Base DVD 在 `3s5z_vs_3s6z` 上的不稳定和固定 DVD 的跨任务负迁移，而不是删除原方法。

### 最新证据

`3s5z_vs_3s6z` 的严格 paired-seed 实验表明：

| 方法 | seed79--81 AUC | seed79--81 的 9--10M 胜率 |
|---|---:|---:|
| Scheduled Takeover | 0.3714 | 0.6241 |
| Floor0 Takeover | 0.4595 | 0.6653 |
| matched-BM | **0.4852** | **0.7881** |

Floor0 在 1--3M、3--5M 分别达到 0.19、0.52，略高于 BM 的 0.17、0.49；但 7M 后落后。这说明弱 DVD 是有用的早期信用脚手架，永久 DVD 偏置则会妨碍收敛。seed82 在三种方法中都失败，因此属于共享 BM/探索盆地的失败轨迹，正式统计中继续保留。

### 当前最终候选：Adaptive Takeover

核心公式：

\[
W_1=(1-g_\theta(z))W_1^{BM}+g_\theta(z)\widehat W_1^{DVD}.
\]

`g_theta` 根据 state、attention entropy、head disagreement、BM/DVD cosine 和 RMS distance 逐样本学习。它不输入 `t_env`，不使用人工 warm-up、ramp、release 或 reliability floor，直接由 TD loss 端到端训练。

新增实现：

- `modules/mixers/dvd_adaptive_takeover.py`
- `config/algs/dvd_adaptive_takeover+qmix_without_abs.yaml`
- `learners/dvd_nq_learner_with_sarsa.py` 中的新 mixer 和日志接入
- `_test_dvd_adaptive_takeover.py`

第一轮实验固定使用 seed79--82，不做超参数网格：

```bash
CUDA_VISIBLE_DEVICES=0 python3 src/main.py --config=dvd_adaptive_takeover+qmix_without_abs --env-config=sc2 with env_args.map_name=3s5z_vs_3s6z use_tensorboard=True seed=79 name=adaptive_takeover_3s5z_s79
CUDA_VISIBLE_DEVICES=1 python3 src/main.py --config=dvd_adaptive_takeover+qmix_without_abs --env-config=sc2 with env_args.map_name=3s5z_vs_3s6z use_tensorboard=True seed=80 name=adaptive_takeover_3s5z_s80
CUDA_VISIBLE_DEVICES=2 python3 src/main.py --config=dvd_adaptive_takeover+qmix_without_abs --env-config=sc2 with env_args.map_name=3s5z_vs_3s6z use_tensorboard=True seed=81 name=adaptive_takeover_3s5z_s81
CUDA_VISIBLE_DEVICES=3 python3 src/main.py --config=dvd_adaptive_takeover+qmix_without_abs --env-config=sc2 with env_args.map_name=3s5z_vs_3s6z use_tensorboard=True seed=82 name=adaptive_takeover_3s5z_s82
```

成功标准：后期先恢复到 matched-BM 附近，同时保留 Floor0 的早期优势；随后使用完全相同配置回到 `6h_vs_8z` 验证跨地图泛化。

---

## 以下为早期研究记录（保留用于追溯）

---

## 一、研究问题

**去除 IGM 约束后，DVD 的轨迹图/后门调整能否缓解多智能体强化学习中的相对过泛化问题？**

即：将 Beyond Monotonicity（BM, AAAI 2026）的"动力学自纠偏"与 DVD（ICML 2022）的"因果去混淆"叠加，观察是否优于纯无约束 QMIX。

---

## 二、当前方法

### 2.1 核心组合

| 来源 | 组件 | 代码位置 |
|---|---|---|
| DVD | 多头 GAT 轨迹图 + 后门调整（D=8 head 均值） | `modules/mixers/dvd.py` MultiHeadGAT + DVDMixer |
| DVD | `abs=False` 去掉单调约束 | `dvd.py:164-165`，由 yaml `abs: False` 控制 |
| BM | SARSA 目标（用 buffer 实际动作，非 max） | `dvd_nq_learner_with_sarsa.py:228-242` |
| BM | TD(λ=0.3) 平滑回报 | `dvd_nq_learner_with_sarsa.py:287-288` |
| BM | RND 内在奖励（归一化 + clip ±5 + β 线性衰减） | `dvd_nq_learner_with_sarsa.py:139-186` |

### 2.2 自主设计选择

- **移除残差连接**（`dvd.py:97`）：mixer 权重完全由 GAT 输出决定，不混入原始 hidden state → 更纯粹的去混淆，但 GAT 没学好时 mixer 信号弱
- **小初始化 ×0.01**（`dvd.py:134,140`）：防止非单调 mixer 初始震荡，但拖慢前期学习
- **软更新 τ=0.01**（`dvd_nq_learner_with_sarsa.py:58,318`）：替代硬更新，更保守

---

## 三、实验现状

### 3.1 已有发现

| 观察 | 说明 |
|---|---|
| **跨种子稳定性好** | 在统一参数下，本方法对种子不敏感；两篇 base 在不同种子上差距大 |
| **收敛慢** | 相比 DVD 原版收敛明显更慢 |

### 3.2 收敛慢的原因分析

1. **小初始化 ×0.01**：前期 mixer 几乎只输出 V(s)，agent 个体 Q 值贡献被压缩，梯度传递弱
2. **无残差连接**：mixer 100% 依赖 GAT 输出，GAT 没学好时整个 mixer 是噪声
3. **软更新 τ=0.01 太保守**：target network 严重滞后，TD target 信息量低

### 3.3 稳定性好的原因分析

1. 多头均值（8 head）天然做方差缩减
2. SARSA 去掉 max 的高估方差
3. 非单调 mixer 不会被锁死在错误的单调结构里，减少"结构性运气"

---

## 四、待修复的代码问题

> 详见 `代码审查纲要.md`，按优先级排列

| # | 问题 | 优先级 | 影响 |
|---|---|---|---|
| 1 | RND 实现两版不对称（主实验归一化+衰减 vs 对照组原始 MSE） | 高 | 实验对比不公平 |
| 2 | 目标网络更新不一致（软更新 vs 硬更新） | 高 | 实验对比不公平 |
| 3 | 对照组缺 `use_rnd` 门控，无法关闭 RND 做消融 | 高 | 无法做消融实验 |
| 4 | `dvd_nq_learner_with_sarsa.py:64` Mixer(args) 默认 abs=True | 中 | 潜伏 bug |
| 5 | 死配置清理（`use_causal_loss`、重复 yaml 键等） | 低 | 代码卫生 |

---

## 五、创新方向探索

### 当前问题

当前方法本质是 DVD + BM 的组件拼接（A+B），两者之间没有因为组合而产生新机制，创新性不足。

### 方向 1：因果感知探索（推荐，创新性最强）

**想法**：用 GAT 的 attention 熵作为探索信号——attention 均匀（熵高）说明因果结构不清楚，需要更多探索；attention 集中（熵低）说明因果已清晰，减少探索。

```
r_explore = β_rnd * r_rnd + β_causal * H(attention)
```

- RND 探索"状态新颖性"，attention 熵探索"因果结构不确定性"——两个正交维度
- 形成闭环：去混淆不仅帮助值分解，还反向指导探索

### 方向 2：渐进式去约束（推荐，最容易出结果）

**想法**：训练初期保持单调约束（快收敛），后期逐渐放开（突破局部最优）。

```python
abs_weight = max(0, 1 - t_env / anneal_steps)
w1_heads = abs_weight * th.abs(w1_heads) + (1 - abs_weight) * w1_heads
```

- 直接解决"收敛慢"问题
- Narrative：单调约束是有用的归纳偏置（prior），但不应永远保留

### 方向 3：多头竞争信用分配

**想法**：D 个 head 不取简单均值，而是根据各 head 的 TD error 加权——预测更准的因果假说权重更高。

```python
head_weights = softmax(head_confidence)
w1 = (w1_heads * head_weights).sum(dim=1)
```

---

## 六、理论证明路线

### 目标

不只做实验，要提供数学证明支撑，参考 BM（AAAI 2026）的理论框架。

### BM 的理论基础

> BM 定理条件：mixer f_mix 局部 Lipschitz + Jacobian ∂Q_tot/∂Q_i 满秩（非退化）+ 近似贪心探索
> BM 定理结论：策略改进方向正确 → 收敛到最优联合策略

### 可证明的定理

**定理 1（相容性，热身，~1 页）**

DVDMixer(abs=False) 满足 BM 定理的条件 → 收敛保证继承。

证明思路：逐项验证 Lipschitz（Linear+ELU 复合）+ Jacobian 满秩（Xavier 初始化 + GAT 泛型非零）。

**定理 2（方差缩减，核心贡献，~2 页）**

设 J_D = (1/D)Σ J_d 为 D-head DVD 的 Jacobian 估计，则：
- Var[J_D] = Var[J_1] / D
- 策略梯度的 L2 误差以 O(1/√D) 衰减

证明思路：独立采样 + 方差分解（标准概率论）。

实验验证：dvd_heads 从 1→2→4→8，跨种子方差应单调下降。

**定理 3（无偏性，锦上添花，~2 页）**

在 DVD 的因果图假设下，E[J_D] 等于后门调整下的因果效应，而普通 hypernet 的 Jacobian 包含混淆偏差项 Δ_conf。

证明思路：展开后门调整公式，对比有/无 GAT 的 Jacobian。难度较大，可先作为 discussion 中的非正式论证。

### 理论与实验的对应

| 定理 | 实验验证 | 对应发现 |
|---|---|---|
| 定理 2（方差缩减） | dvd_heads=1,2,4,8 消融 | 解释跨种子稳定性 |
| 定理 3（无偏性） | DVD vs 无 DVD 的梯度 cosine similarity | 解释去混淆的必要性 |

---

## 七、下一步工作优先级

### 短期（代码修复 + 快速实验）

1. 统一对照组实验基线（RND、目标更新）
2. 修复 Mixer(args) 默认 abs=True 的潜伏 bug
3. 跑 `dvd_heads=1,2,4,8` 消融 → 验证方差缩减假说

### 中期（方法改进 + 理论）

4. 实现渐进式 abs 退火 → 解决收敛慢问题
5. 实现因果感知探索（attention 熵）→ 核心创新点
6. 写定理 1+2 的严格证明

### 长期（论文 + 完整实验）

7. 完整实验矩阵：QMIX / NM-QMIX / DVD-QMIX / DVD+NM，≥5 seeds
8. 补消融格：DVD(abs=True) + SARSA/RND → 排除"增益来自 GAT 结构而非交互"
9. 写作：理论（定理 1-3）+ 实验 + 分析
